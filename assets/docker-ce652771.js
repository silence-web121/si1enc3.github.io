const r='# 安装\r\n\r\n## Ubuntu\r\n\r\n操作系统官方软件源中默认自带Docker包，可直接安装使用\r\n\r\n## windows\r\n\r\n直接在官网下载。这里补充一下汉化版\r\n\r\n我下的4.82.0所以https://github.com/asxez/DockerDesktop-CN/releases/tag/4.82.0里下`app-Windows-x86.zip`\r\n\r\n1. 退出 Docker Desktop\r\n右下角托盘找到 Docker 图标，右键点 **Quit Docker Desktop**。\r\n\r\n2. 打开这个目录：\r\n\r\n```Plain Text\r\nC:\\Program Files\\Docker\\Docker\\frontend\\resources\r\n```\r\n\r\n3. 备份这两个东西：\r\n\r\n```Plain Text\r\napp.asar\r\napp.asar.unpacked\r\n```\r\n\r\n可以改名成：\r\n\r\n```Plain Text\r\napp.asar.bak\r\napp.asar.unpacked.bak\r\n```\r\n\r\n4. 把zip解压出来的：\r\n\r\n```Plain Text\r\napp.asar\r\napp.asar.unpacked\r\n```\r\n\r\n复制到：\r\n\r\n```Plain Text\r\nC:\\Program Files\\Docker\\Docker\\frontend\\resources\r\n```\r\n\r\n6. 还要替换这个文件：\r\n\r\n```Plain Text\r\nC:\\Program Files\\Docker\\Docker\\frontend\\Docker Desktop.exe\r\n```\r\n\r\n先把原来的 `Docker Desktop.exe` 备份成：\r\n\r\n```Plain Text\r\nDocker Desktop.exe.bak\r\n```\r\n\r\n再把压缩包里解压出来的 `Docker Desktop.exe` 复制到：\r\n\r\n```Plain Text\r\nC:\\Program Files\\Docker\\Docker\\frontend\\\r\n```\r\n\r\n> 注意：保留根目录启动器不动：\r\n> \r\n> ```Plain Text\r\n> C:\\Program Files\\Docker\\Docker\\Docker Desktop.exe\r\n> ```\r\n> \r\n> \r\n\r\n7. 重新打开 Docker Desktop就好。\r\n\r\n\r\n\r\n# 三大核心\r\n\r\n## 镜像\r\n\r\n一个把软件和运行环境一起打包好的只读模板。由多层组成，每一层记录一次文件系统变化，方便复用和缓存。\r\n\r\n里面通常包含：\r\n\r\n- 应用程序代码\r\n\r\n- 运行依赖\r\n\r\n- 系统库\r\n\r\n- 配置文件\r\n\r\n- 启动命令\r\n\r\n- 一个最小化的 Linux 文件系统\r\n\r\n\r\n\r\n### 常用命令\r\n\r\n1. 搜索镜像：`docker search 关键字`\r\n\r\n2. 获取镜像：`docker pull <镜像名>:<标签>`（从仓库下载镜像）\r\n\r\n3. 构建镜像：`docker build -t <名称:标签> .`（最后的.不能忘）\r\n\r\n4. 查看镜像：`docker images`（常用于验证是否构建成功）\r\n\r\n5. 删除镜像：`docker rmi <镜像ID或名称>`\r\n\r\n\r\n\r\n> 注意：标签不加也能跑，不指定版本标签时，Docker 默认用 `latest`。实际开发里还是建议加标签，尤其是部署、比赛环境、生产环境。便于复现\r\n> \r\n> \r\n\r\n### Dockerfile\r\n\r\n一个普通的文本文件，其中包含了一条条的指令，每一条指令都会构建一层，基于基础镜像，最终构建出一个新的镜像。镜像通常来自 Dockerfile，Dockerfile 描述怎么一步步构建镜像。\r\n\r\n#### 指令\r\n\r\n```Python\r\nFROM            #基础镜像，一切从这里开始构建\r\nMAINTAINER      #镜像是谁写的，姓名+邮箱\r\nRUN             #镜像构建时需要运行的命令\r\nADD             #步骤，tomcat镜像，这个tomcat压缩包；添加内容\r\nWORKDIR         #镜像工作目录\r\nVOLUME          #挂载的目录\r\nEXPOSE          #暴露端口配置\r\nCMD             #指定这个容器启动的时候要运行的命令,只有最后一个会生效，可被替代\r\nENTRYPOINT      #指定这个容器启动的时候要运行的命令，可以追加命令\r\nONBUILD         #当构建一个被继承DockerFile 这个时候就会运行 ONBUILD 的指令，触发指令\r\nCOPY            #类似ADD,将我们的文件拷贝至镜像中\r\nENV             #构建的时候设置环境变量\r\n```\r\n\r\n1. -t 参数用来指定镜像的命名空间，仓库名以及 TAG 等信息。\r\n\r\n2. 最后面的 `.` 是指镜像构建上下文。\r\n\r\n\r\n\r\n## 容器\r\n\r\n**隔离**：每个容器有自己的文件系统、进程空间、网络环境。\r\n\r\n**轻量**：不像虚拟机那样要完整操作系统内核，容器共享宿主机内核。\r\n\r\n**可删除重建**：容器坏了删掉，用同一个镜像再起一个。\r\n\r\n**可运行多个**：一个镜像可以启动很多个容器。\r\n\r\n\r\n\r\n### 常用命令\r\n\r\n1. 查看容器：\r\n\r\n- `docker ps`（正在运行的）\r\n\r\n- `docker ps -a`（所有的，包括已经关闭的）\r\n\r\n- `docker logs <容器ID或名称>`（输出日志）\r\n\r\n- `docker top`（进程）\r\n\r\n- `docker stats`（资源占用）\r\n\r\n- `docker inspect id|name`（详情）\r\n\r\n- `docker inspect <容器ID> | grep "IPAddress"`（看ip）\r\n\r\n\r\n\r\n2. 创建容器：\r\n\r\n- `docker create <镜像名>`\r\n\r\n- `Docker run <镜像名>`（创建并启动）\r\n\r\n\r\n\r\n3. 启动容器：\r\n\r\n`docker start <容器ID或名称>`\r\n\r\n\r\n\r\n4. 关闭容器：\r\n\r\n`docker stop <容器ID或名称>`\r\n\r\n\r\n\r\n5. 重启容器：\r\n\r\n`docker restart <容器ID或名称>`\r\n\r\n\r\n\r\n6. 重命名容器：\r\n\r\n`docker rename <旧名> <新名>`\r\n\r\n\r\n\r\n7. 复制文件到容器:\r\n\r\n`docker cp /host/file.txt container_id:/container/path/`\r\n\r\n\r\n\r\n8. 进入容器内部执行命令：\r\n\r\n`docker exec -it <容器ID> /bin/bash`\r\n\r\n\r\n\r\n9. 导出一个已经创建的容器导到一个文件\r\n\r\n`docker export -o 文件名.tar 容器id`\r\n\r\n将文件导入为镜像\r\n`docker import 文件名.tar 镜像名:镜像标签`\r\n\r\n\r\n\r\n10. 删除容器：\r\n\r\n- `docker rm <容器ID或名称>`（已停止）\r\n\r\n- `docker rm -f <容器ID>`（正在运行）\r\n\r\n\r\n\r\n## 镜像和容器的联系\r\n\r\n1. **镜像**是容器运行的基础，**容器**是镜像运行后的形态。\r\n\r\n2. 一个镜像可以启动多个容器。\r\n\r\n- 镜像是静态的：像安装包，模板。\r\n\r\n- 容器是动态的：镜像运行起来之后就是容器。\r\n\r\n\r\n\r\n## 仓库\r\n\r\n存放镜像文件的地方。比如maven仓库是存放jar包的地方。\r\n\r\n\r\n\r\n# 数据卷与挂载\r\n\r\n避免容器删除后数据丢失，使用数据卷与目录挂载。\r\n\r\n数据卷 = 宿主机和容器共用的文件夹\r\n\r\n比如：\r\n\r\n```Plain Text\r\n宿主机目录：D:\\web\r\n容器目录：/var/www/html\r\n```\r\n\r\n把它们挂载起来后：\r\n\r\n```Plain Text\r\nD:\\web 里的文件就等于容器里 /var/www/html 的文件\r\n```\r\n\r\n## 常用命令\r\n\r\n1. 创建数据卷：`docker volume create <卷名>`\r\n\r\n2. 查看所有数据卷：`docker volume ls`\r\n\r\n3. 查看数据卷详情：`docker volume inspect`\r\n\r\n4. 删除数据卷：`docker volume rm <卷名>`\r\n\r\n5. 删除所有未使用的卷：`docker volume prune`\r\n\r\n\r\n\r\n## 挂载方式（在run中使用）\r\n\r\n```PHP\r\n# 方式1：挂载主机目录（推荐开发环境）\r\ndocker run -d -v /host/path:/container/path nginx\r\n\r\n# 方式2：使用命名卷（推荐生产环境）\r\ndocker run -d -v mydata:/data nginx\r\n\r\n# 方式3：只读挂载\r\ndocker run -d -v /host/config:/etc/config:ro nginx\r\n```\r\n\r\n# 网络管理\r\n\r\n容器之间通信靠网络。可以使容器可通过名称通信，无需记住 IP。\r\n\r\n通常有三种模式：bridge，host，none\r\n\r\n\r\n\r\n## 常用命令\r\n\r\n1. 创建网络：`docker network create <网络名> `\r\n\r\n2. 查看网络：`docker network ls`\r\n\r\n3. 将容器加入网络：`docker network connect <网络><容器>`\r\n\r\n4. 将容器移除网络：`docker network disconnect <网络><容器>`\r\n\r\n5. 删除网络：`docker network rm <网络名>`\r\n\r\n```PHP\r\n# 启动两个容器并加入同一网络\r\ndocker run -d --name db --network mynet redis\r\ndocker run -d --name app --network mynet myapp:v1\r\n\r\n# 此时 app 容器可以直接用 `db` 作为主机名访问 Redis\r\n```\r\n\r\n\r\n\r\n\r\n\r\n# Docker-compose（强推）\r\n\r\n用 `docker-compose.yml` 文件一键管理多个服务，比如要启动哪些容器、端口、环境变量、数据卷、网络关系等等。用一条命令启动全部就不用一条一条docker命令敲了。它适用于一个项目需要多个服务一起跑。\r\n\r\n```Plain Text\r\n比如：Web + MySQL + Redis + Nginx\r\n```\r\n\r\nDocker：一个个启动容器\r\nDocker Compose：按配置文件一键启动一组容器\r\n\r\n\r\n\r\n## 常用命令\r\n\r\n1. 启动服务：\r\n\r\n- `docker-compose up`（前端运行）\r\n\r\n- `docker-compose up -d`（后端启动）\r\n\r\n2. 查看服务：\r\n\r\n- `docker-compose ps`（看状态）\r\n\r\n- `docker-compose logs`（看日志）\r\n\r\n3. 重构服务：`docker-compose build`\r\n\r\n4. 停止并删除所有服务：`docker-compose down`\r\n\r\n\r\n\r\n# 清理与维护\r\n\r\n定期清理可以释放磁盘空间\r\n\r\n\r\n\r\n## 常用命令\r\n\r\n1. 查看docker磁盘使用情况：`docker system df`\r\n\r\n2. 清理所有未使用资源：`docker system prune`（容器、网络、镜像、构建缓存）\r\n\r\n3. 清理所有未使用镜像：`docker system prune`（包括未被引用的）\r\n\r\n4. 清理构建缓存：`docker builder prune`\r\n\r\n> `prune -a` 会删除所有未使用的镜像，确认后再执行！\r\n> \r\n> \r\n\r\n\r\n\r\n# 总结\r\n\r\n|类别       |常用命令|\r\n|---|---|\r\n|镜像|images，pull，build，rmi|\r\n|容器|run，ps，start/stop，exec，logs|\r\n|数据|volume create，-v 挂载 |\r\n|网络|networkcreate，--network|\r\n|编排|Docker-compose up/down|\r\n|清理|system prune，volume prune |\r\n\r\n\r\n\r\n';export{r as default};
